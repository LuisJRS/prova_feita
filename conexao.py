import mysql.connector

con = mysql.connector.connect(
                                host = 'localhost',
                                database = 'cad_cliente',
                                user = 'root',
                                password = '@Luis300615'

)